@model IEnumerable<MPGTracker.Models.OilChange>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MilesOnCar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfChange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Car.Owner)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MilesOnCar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfChange)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Car.Owner)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
